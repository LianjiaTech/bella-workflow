#-------------------------------------------------------
# 服务启动配置文件
#-------------------------------------------------------
server:
  port: ${PORT:8080}
spring:
  application:
    name: bella-workflow
